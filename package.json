{
  "name": "freedom-chess-extension",
  "version": "2.0.0",
  "private": true,
  "description": "Extensão de controle por voz para o Chess.com.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/build-extension.mjs check",
    "build": "node scripts/build-extension.mjs build",
    "package": "node scripts/build-extension.mjs package"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "ISC"
}
